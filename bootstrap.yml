---
- hosts: all
  roles:
    - locale
    - grub
    - users
    - { role: packages, packages: ['git', 'openssh'] }
    - { role: virtualbox-guest, when: ansible_virtualization_type == "virtualbox" }
