---
- name: Playbook to run adhoc roles
  hosts: "{{ target_hosts }}"
  gather_facts: true

  tasks:
    - name: Include roles
      include_role:
        name: "{{ item }}"
      with_items:
        - "{{ role_name.split(',')|list }}"
