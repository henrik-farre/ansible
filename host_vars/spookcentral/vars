---
duplicity:
  dump: "daily"
  clean: "weekly"
  remove-old: "weekly"
  backup: "21:00"
  volumes:
    - shared/pictures
    - shared/clips
    - shared/enrique
    - shared/mythtv
    - shared/backup
    - usr/local
    - etc
    - var
  excludes:
    - { src: "home", dest: "/shared/enrique" }
    - { src: "home", dest: "/shared/mythtv" }
    - { src: "var", dest: "/var" }
  passphrase: "{{ vault_duplicity.passphrase }}"
  encrypt_key: "{{ vault_duplicity.encrypt_key }}"
  remote_dir_base: "{{ vault_duplicity.remote_dir_base }}"
  remote_host: "{{ vault_duplicity.remote_host }}"

networks:
  - "spookcentral_eth0"

users_home_base: "shared"

modules_load_d:
  - bluetooth-protocol.conf

modules_modprobe_d:
  - 00-blacklist.conf
  - 51-sound-hdmi-analog.conf
