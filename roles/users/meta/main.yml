---
dependencies:
  - { role: packages, packages: ['zsh', 'zsh-completions'], when: ansible_distribution == "Archlinux" or ansible_distribution == "Arch Linux" }
  - { role: packages, packages: ['zsh'], when: ansible_distribution == "Debian" }
  - role: sudo
