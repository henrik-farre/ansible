---
- name: Ensure Docker and related packages are installed
  become: true
  community.general.pacman:
    name: "{{ packages }}"
    state: latest
  vars:
    packages:
      - docker
      - docker-compose
      - python-docker
  tags:
    - docker
