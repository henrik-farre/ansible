---
- name: Ensure Docker and related packages are installed
  community.general.pacman:
    name: "{{ packages }}"
    state: latest
  vars:
    packages:
      - docker
      - docker-compose
      - python-docker
