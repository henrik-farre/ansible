---
# --base-config ../molecule-resources/base-config-docker.yml
platforms:
  - name: instance
    pre_build_image: true
    image: "rockhopperdk/docker-archlinux-systemd:latest"
provisioner:
  name: ansible
