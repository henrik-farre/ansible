---
- name: Install packages
  become: yes
  pacman:
    name: "{{ item }}"
    update_cache: yes
    state: latest
  with_items:
    - "{{ packages }}"
  when: ansible_distribution == "Archlinux"
  tags:
    - packages
