---
- name:
  become: yes
  hostname:
    name: "{{ ansible_hostname }}"

- name: add hostname to /etc/hosts
  become: yes
  template:
    src: etc/hosts.j2
    dest: /etc/hosts
    owner: root
    group: root
    mode: 0644
