---
- name:
  hostname:
    name: "{{ short_hostname }}"

- name: add hostname to /etc/hosts
  lineinfile:
    dest=/etc/hosts
    line='127.0.1.1 {{ full_hostname }} {{ short_hostname }}'
    regexp='^127\.0\.1\.1'
    insertafter='^127\.0\.0\.1'
    state=present
