---
- name: Placeholder for Gnome role
  pacman:
    name: "{{ packages }}"
    state: latest
  vars:
    packages:
      - gdm
      - gnome-shell
      - gnome-shell-extensions
      - gnome-shell-extension-gtile
      - gnome-menus
      - gnome-control-center
  tags:
    - gnome
