---
homeassistant:
  hostname: "ha.{{ inventory_hostname }}.{{ network.local_domain }}"
  ip: "{{ ansible_facts.default_ipv4.address }}"
  db:
    root_password: "{{ vaulted_ha.db.root_password }}"
    database: "{{ vaulted_ha.db.database }}"
    user: "{{ vaulted_ha.db.user }}"
    password: "{{ vaulted_ha.db.password }}"
  zigbee2mqtt:
    hostname: "zigbee2mqtt.{{ inventory_hostname }}.{{ network.local_domain }}"
    devices:
      - /dev/serial/by-id/usb-ITEAD_SONOFF_Zigbee_3.0_USB_Dongle_Plus_V2_20220811181030-if00
  mosquitto:
    user: "{{ vaulted_ha.mosquitto.user }}"
    password: "{{ vaulted_ha.mosquitto.password }}"
    salt: "{{ vaulted_ha.mosquitto.salt }}"
