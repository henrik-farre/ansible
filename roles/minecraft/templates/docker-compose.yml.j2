services:
  mc:
    image: {{ containers.minecraft.repo }}:{{ containers.minecraft.tag }}
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
    volumes:
      - /srv/minecraft/data:/data
