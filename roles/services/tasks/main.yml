---
- name: Reload systemd
  become: yes
  command: systemctl daemon-reload
  tags:
    - services

- name: Enable services
  become: yes
  systemd:
    name: "{{ item }}"
    enabled: yes
  with_items:
    - "{{ services }}"
  tags:
    - services
