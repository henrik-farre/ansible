---
- name: Reload systemd
  become: yes
  ansible.builtin.systemd:
    daemon_reload: yes
  tags:
    - services

- name: Enable services
  become: yes
  ansible.builtin.systemd:
    name: "{{ item }}"
    enabled: yes
  with_items:
    - "{{ services }}"
  tags:
    - services
