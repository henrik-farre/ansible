---
- hosts: all
  become: yes
  tasks:
    - name: Update system packages
      pacman:
        update_cache: yes
        upgrade: yes

    - name: Install neovim-remote
      pip:
        name: neovim-remote
        extra_args: --user
        executable: pip3
        state: latest
