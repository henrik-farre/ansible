---
- hosts: all
  roles:
    - ntp
    - cron
    - ssh_keys
    - backup
    - dotfiles
    - vim
    - docker

- hosts: desktop
  roles:
  - xorg
  - desktop
  - fonts
  - zim
  - { role: package, packages: ['smartmontools'] }
  - { role: services, services: ['smartd', 'alsa-restore'] }
