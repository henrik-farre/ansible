---
- hosts: all
  roles:
    - ntp
    - cron
    - ssh_keys
    - backup
    - dotfiles
    - vim
    - docker

- hosts: desktop
  roles:
  - xorg
  - desktop
  - fonts
  - zim
  - { role: services, packages: ['smartd', 'alsa-restore'] }
